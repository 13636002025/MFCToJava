

		心得体会：

		在此静态代理中，用一个猫的代理类去代理猫的动作，如果需要改方法的内容的话只需要在Cat类中修改方法的
	内容其他的地方都不需要变动，但是要是想要再加入类的话，比如想要加入一个Dog类，然后方法发生相应的变化，
	那么整个程序的很多地方都要改动，代理类也得重新创建，这就是静态代理的弊端。
		而动态代理中，代理对象是在运行时才创建目标类的代理对象，因此，加入一个新的类的时候，不必再创建一个
	代理对象，只需将此类连接在公共接口上，然后在主函数中进行所需要的编码即可。